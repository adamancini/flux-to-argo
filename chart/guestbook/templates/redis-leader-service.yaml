apiVersion: v1
kind: Service
metadata:
  name: redis-leader
  labels:
    app: redis
    role: leader
    tier: backend
spec:
  ports:
    - port: {{ .Values.redisLeader.service.port }}
      targetPort: 6379
  selector:
    app: redis
    role: leader
    tier: backend
