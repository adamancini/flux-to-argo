apiVersion: v1
kind: Service
metadata:
  name: redis-follower
  labels:
    app: redis
    role: follower
    tier: backend
spec:
  ports:
    - port: {{ .Values.redisFollower.service.port }}
      targetPort: 6379
  selector:
    app: redis
    role: follower
    tier: backend
